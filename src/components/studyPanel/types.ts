// All previous study panel tab types were removed alongside the StudyPanelModal
// tab UI in the visual-clutter cleanup. This file is intentionally a stub so that
// any stale `import ... from './types'` lines fail loudly at build time rather
// than silently re-exporting something unexpected.
export {};
